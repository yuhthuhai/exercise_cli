declare global {
    interface Date {
        addHours(hours: number): Date;
        addDays(days: number): Date;
    }
    interface DateConstructor {
        utc: () => Date;
        utcByDays: (days: number) => Date;
    }
}
export {};
