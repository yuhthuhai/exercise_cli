declare global {
    interface String {
        capitalize(): string;
        camelCase(): string;
        isGuid(): boolean;
        isValidURL(): boolean;
        isJSON(): boolean;
        safeURI(path: string): string;
        fullMatch(des: string): boolean;
    }
}
export {};
